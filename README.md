# ioBroker.open-wa

## SendTo support
This adapter supports `sendTo("open-wa.X","send",{to,content})` (messagebox enabled).

If messages are not delivered:
- Check adapter log (set loglevel to debug)
- Ensure gateway reachable
